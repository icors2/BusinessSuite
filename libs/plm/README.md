# plm

Product Lifecycle Management — document control with immutable revision history.

## Events

See [EVENTS.md](./EVENTS.md).

## Usage

Import `PlmModule` and inject `DocumentService` for metadata and lifecycle operations.
Binary upload/download is handled by `DocumentsController` in the API app.
