# wms

Warehouse Management System — locations, bins, inventory quantities, and movements.

## Events

See [EVENTS.md](./EVENTS.md).

## Usage

Import `WmsModule` and inject `LocationService` / `InventoryService` for warehouse operations.
