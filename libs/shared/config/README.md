# Shared Config Library

Typed environment configuration loader for all environments.

## Exports

- `loadAppConfig()` — loads and validates env vars for development/staging/production
- `AppConfig`, `NodeEnv` types

## Environment variables

See root [`.env.example`](../../../.env.example).
